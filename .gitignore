# ── Dependencies ──────────────────────────────────────────
node_modules/

# ── Next.js ───────────────────────────────────────────────
.next/
out/
dist/
next-env.d.ts
*.tsbuildinfo

# ── Environment variables (NEVER commit these) ────────────
.env
.env.local
.env.*.local
.env.development
.env.production
.env.staging

# ── Build & Output artefacts ──────────────────────────────
output/
coverage/
build/

# ── Local dev tooling & IDE configs ──────────────────────
# Cursor AI
.cursor/
# VS Code (workspace-specific, not shared)
.vscode/
# Zed
.zed/
# Gemini CLI
.gemini/
# Codex
.codex/
# OpenCode
opencode.json

# ── MCP / AI agent settings (may contain API keys) ───────
cline_mcp_settings.json
# .mcp.json is intentionally committed (no secrets inside)

# ── Drizzle ───────────────────────────────────────────────
.drizzle/

# ── Test artifacts ────────────────────────────────────────
testsprite_tests/tmp/
testsprite_tests/**/*.tmp

# ── Misc ──────────────────────────────────────────────────
cache/
package-lock.backup.json
.vercel
*.log
*.swp
.DS_Store
Thumbs.db

# ── One-off generated files (not part of source) ─────────
aifinanceops-dashboard.html
build-html.js

# ── Internal planning docs (not for public repo) ─────────
PRD.md
PROJECT_PLAN.md
